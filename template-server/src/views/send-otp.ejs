<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Send Otp</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            padding: 0;
            /* display: flex;
            justify-content: center; */
        }

        .container {
            width: 1200px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin: 0 auto;
            text-align: center;
            padding: 20px;
        }

        .form-group {
            text-align: center;
            padding: 20px;
            border-radius: 8px;
        }

        label {
            font-size: 16px;
            font-weight: bold;
            display: block;
            margin-bottom: 10px;
        }

        input {
            width: 20%;
            padding: 10px;
            margin-bottom: 20px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }

        span#error {
            color: red;
            font-size: 14px;
        }

        #emailError {
            color: red;
            font-size: 14px;
        }

        .btn-otp {
            display: flex;
            gap: 20px;
            justify-content: center;
        }

        .restotpemail {
            width: 10% !important;
            background-color: #007bff;
            color: #fff;
            cursor: pointer;
        }

        .errorMessage {
            text-align: center;
            color: red;
            padding: 10px;
        }
    </style>
    <script>
        // function checkOtp() {
        //     var enteredOtp = document.getElementById('otpInput').value;
        //     var errorSpan = document.getElementById('error');
        //     if (!enteredOtp) {
        //         errorSpan.innerText = 'Incorrect OTP';
        //         return false; // Ngăn chặn form từ việc submit
        //     } else {
        //         errorSpan.innerText = '';
        //     }

        // }

        // function checkRestOtp(event) {

        //     var restotpemailOtp = document.getElementById('restOtpInput').value;
        //     var emailErrorSpan = document.getElementById('emailError');
        //     // console.log(restotpemailOtp);
        //     // Kiểm tra ô input của Email
        //     if (!isValidEmail(restotpemailOtp)) {
        //         emailErrorSpan.innerText = 'Please enter a valid email address.';
        //     } else {
        //         emailErrorSpan.innerText = '';
        //     }

        //     // Nếu có lỗi, không thực hiện submit form
        //     if (!isValidEmail(restotpemailOtp)) {
        //         return false;
        //     }

        //     // Nếu dữ liệu hợp lệ, bạn có thể tiếp tục với xử lý form hoặc chuyển hướng
        //     // Ví dụ: document.forms[0].submit(); hoặc window.location.href = "/new-page";
        // }

        // function isValidEmail(email) {
        //     // Simple email validation, you might want to use a more robust solution
        //     var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        //     return emailRegex.test(email);
        // }
    </script>
</head>

<body>
    <div class="container">
        <h1>Verify Otp</h1>

        <div class="form-group">

            <form onsubmit="return checkRestOtp(event)" action="/refresh-otp-email-server" method="post">
                <div>
                    <label for="otpInput">Enter Email</label><br />
                    <input type="text" id="restOtpInput" name="refreshotp" />
                    <input type="submit" value="Refresh OTP" class="restotpemail" /><br />
                    <span id="emailError"></span>
                </div>
                <div>
                    <label for="otpInput">Enter Otp Email</label><br />
                    <input type="number" id="otpInput" name="sendemail" />
                    <button type="submit">Verify OTP</button><br />
                    <!-- <input type="submit" class="restotpemail" value="Verify OTP" /><br /> -->
                    <span id="error"></span>

                </div>
            </form>
        </div>


        <p>Don't have an account? <a href="/login-server">Login</a></p>
    </div>
</body>

</html>